#include <stdio.h>

int i;

int main ()
{
   printf("Los bumeros pares entre el 1 y el 109 son:\n");

 for (i = 1; i<= 100; i++)
 {
    if (i % 2 == 0)
   {
     printf("%d\n", i)
   }
 }
printf("\n");
return 0;
}
